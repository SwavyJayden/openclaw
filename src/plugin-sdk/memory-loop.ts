// Narrow plugin-sdk surface for the bundled memory-loop plugin.
// Keep this list additive and scoped to symbols used under extensions/memory-loop.

export { emptyPluginConfigSchema } from "../plugins/config-schema.js";
export type { OpenClawPluginApi } from "../plugins/types.js";
